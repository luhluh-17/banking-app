class Transaction {
  constructor(id, date, description, status, amount) {
    this.id = id
    this.date = date
    this.description = description
    this.status = status
    this.amount = amount
  }
}
